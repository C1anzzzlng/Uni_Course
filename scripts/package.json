{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "update-school-coords": "tsx ./src/update-school-coords.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "seed-schools": "node --env-file=../lib/db/.env --import tsx ./src/seed-schools.ts"
  },
  "dependencies": {
    "@workspace/db": "workspace:*",
    "drizzle-orm": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "tsx": "catalog:"
  }
}
